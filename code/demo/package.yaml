name: loot-demo

library:
  source-dirs: lib

  dependencies:
    - base-noprelude

    - loot-config
    - loot-prelude
    - loot-log

executables:
  loot-demo:
    source-dirs: app
    main: Main.hs

    dependencies:
      - base-noprelude
      - loot-demo
      - loot-log

      - loot-demo
      - loot-prelude

      - caps
      - fmt
      - loot-config
      - yaml

ghc-options:
  - -Wall
