name: loot-demo

library:
  source-dirs: lib

  dependencies:
    - base-noprelude

    - loot-config
    - loot-prelude

executables:
  loot-demo:
    source-dirs: app
    main: Main.hs

    dependencies:
      - base-noprelude

      - loot-demo
      - loot-prelude

      - loot-config
      - yaml

ghc-options:
  - -Wall
