<<: !include "hpack/lib.yaml"

name: loot-base

library:
  <<: *lib-common

  dependencies:
    - microlens
    - tagged

tests:
  loot-base-test:
    <<: *test-common

    dependencies:
      - tasty
      - tasty-discover
