<<: !include "hpack/lib.yaml"

name: loot-base

library:
  <<: *lib-common

  dependencies:
    - tagged

tests:
  loot-base-test:
    <<: *test-common

    dependencies:
      - tasty
      - tasty-discover
